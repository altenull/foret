import { css } from '@emotion/core';

export const fieldSetStyles = css({
  display: 'inline-flex',
  margin: 0,
  padding: 0,
  border: 'none',
});

export const legendStyles = css({
  padding: 0,
  border: 'none',
  marginBottom: '12px',
});
